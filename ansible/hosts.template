all:
  vars:
    ansible_host: "{{inventory_hostname}}.{{domain}}"
  hosts:
  children:
    haproxy:
      hosts:
        k8s-haproxy:
    kubernetes:
      children:
        controllers:
        workers:
    controllers:
      hosts:
        k8s-controller[1:3]:
    workers:
      hosts:
        k8s-worker[1:3]:
