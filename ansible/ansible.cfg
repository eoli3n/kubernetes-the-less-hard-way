[defaults]
stdout_callback = yaml
forks = 10
pipelining = True
gathering = smart
fact_caching = jsonfile
fact_caching_connection = /tmp/facts_cache.json
fact_caching_timeout = 14400
inventory = ./hosts
host_key_checking = False
remote_user = root
interpreter_python = auto_silent
